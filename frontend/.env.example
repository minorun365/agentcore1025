# ===========================
# Lambda Function URL (クライアントサイドから直接呼び出し)
# ===========================
# CDKデプロイ後の出力から取得し、Amplify Console > App settings > Environment variablesに設定
# ブラウザから直接Lambda Function URLを呼び出します（ストリーミング優先）
#
# 注意: NEXT_PUBLIC_ プレフィックスはクライアントサイドで使用可能にするために必要
# セキュリティ: Lambda URLが公開されるため、誰でも呼び出し可能（コスト攻撃リスクあり）
NEXT_PUBLIC_LAMBDA_FUNCTION_URL=https://YOUR_LAMBDA_FUNCTION_URL.lambda-url.us-west-2.on.aws/

# ===========================
# AgentCore & Tavily (Lambda環境変数として設定 - infrastructure/.envに設定)
# ===========================
# これらはLambda関数の環境変数として設定されるため、frontendの環境変数としては不要
# infrastructure/.env に記載してCDKデプロイ時にLambdaに設定されます

# AgentCore Runtime ARN (Lambda環境変数)
# AGENT_RUNTIME_ARN=arn:aws:bedrock-agentcore:us-west-2:YOUR_AWS_ACCOUNT_ID:runtime/strandsAgent-XXXXXXXXXX

# Tavily API Key (Lambda環境変数)
# TAVILY_API_KEY=tvly-YOUR_API_KEY

# ===========================
# ローカル開発用 (オプション)
# ===========================
# ローカル開発時にNext.js API Routeから直接AgentCoreを呼び出す場合のみ必要
# 本番環境(Amplify Hosting)では不要

# AWS CLIプロファイル (SSOログイン済みプロファイルを指定)
# AWS_PROFILE=your-profile-name

# AWSリージョン
# AWS_REGION=us-west-2
